Камера - не открываеться по примеру с методы, вместо этого будет откриваться приложение контактов.
Если нужна именно камера отпиши.

***
В качестве фишки - генератор случайных чисел - добавляет вкладки програмным способом.
***
Также используются локализация через strings.xml
Ну и метод private void openApp(String category) вместо дубликации кода.

***
В приложении есть пакет ui и куча мусора с приложения которое создается по умолчанию(не удалял)